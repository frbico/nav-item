# 安全修复与部署要求

本版本修复了上游已确认的安全问题：

- 使用显式部署 JWT 密钥，检查算法、签发方、接收方、用户存在性和会话版本；移除固定签名密钥。
- 改密及退出登录撤销全部账号会话；前端处理过期会话。
- 登录限速，不信任客户端转发 IP；管理员初始化不再使用默认密码。
- 上传先鉴权和限速，限制 multipart 大小与数量，用 Sharp 校验、重新编码图片并随机命名；不提供历史 HTML/SVG 上传文件。
- 严格验证密码类型和长度，使用异步密码哈希，避免畸形请求结束进程。
- 校验分页、导航 URL、卡片归属；开启 SQLite 外键级联约束；客户端拦截旧数据中的可执行 URL 协议。
- 安全响应头、默认同源策略；非 root 容器，移除运行阶段 npm 工具。
- 升级 Multer、SQLite 和前端依赖；更新 Debian/Node 基础镜像和系统包。

功能修复包括：远端自定义图标路径、子菜单搜索、搜索结果定位、图片失败占位符及会话过期显示。

旧数据库会原位增加会话版本字段。升级前应备份；不会自动清除历史孤立数据或重置已有密码。

扫描工具的命中不等于已证实可远程利用，且未发现后门不等于绝对安全。请持续升级依赖，关注构建结果及后续公告。
历史 `ct8-and-serv00` Release 附件已替换为本仓库修复版；完整说明见 [审计报告](docs/SECURITY-AUDIT.md)。

## 本次验证（2026-09-15）

- 虚拟机安全回归测试：9 项全部通过；包含旧 users 表结构迁移且保留现有密码。
- 前后端 npm audit：均为 0 个已知依赖漏洞命中。
- AMD64 运行镜像 Trivy：Node.js 依赖命中 0，Debian 13 系统包仍有 43 条 high、48 条 medium、56 条 low、1 条 unknown 记录；critical 为 0。这些剩余记录没有扫描器标注的可用修复版本，且并非已证实本站可远程利用。基础包已更新到仓库当前版本，后续仍需跟踪上游修复。
- ARM64 通过发布工作流的构建和启动验证；完整系统包扫描目前仅在 AMD64 上执行。

不要将以上检查解读为对所有第三方代码或未来漏洞的绝对保证。
